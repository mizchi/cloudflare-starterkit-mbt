# Extra paths to skip in addition to .gitignore (`--secretlintignore .gitignore`
# is passed by the npm script). Encrypted env files are intentionally
# committed, so let secretlint scan them — dotenvx-encrypted values
# decrypt to ciphertext, which the recommend preset will not flag.
# Anything that legitimately contains test/example fixtures with
# secret-shaped strings (e.g. an OpenAPI sample, a docs fragment) goes
# here.

# pnpm lockfile contains nothing secret but the recommend preset can
# generate false positives on integrity hashes; skip for speed.
pnpm-lock.yaml
